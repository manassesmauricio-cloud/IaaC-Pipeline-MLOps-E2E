# ── Copia este archivo a .env y ajusta los valores ──────────────────────────
# cp .env.example .env

# Ruta HOST a los datos crudos (carpeta con data de entrenamiento y OOT)
# Los scripts del pipeline están en ./scripts/ dentro del repo — no requiere configuración.
HOST_DATASET_PATH=/ruta/a/tu/Dataset

# Airflow
AIRFLOW_UID=50000
AIRFLOW__CORE__FERNET_KEY=46BKJoQYlPPOexq0OhDZnIlNepKFf87WFwLt0nfdW8Q=
AIRFLOW__WEBSERVER__SECRET_KEY=mlops-secret-2024

# MLflow
MLFLOW_TRACKING_URI=http://mlflow:5000
MLFLOW_EXPERIMENT_NAME=pipeline-extrac

# Postgres (compartido: Airflow + MLflow)
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow
